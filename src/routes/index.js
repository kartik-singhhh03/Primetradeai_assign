const express = require("express");

const router = express.Router();

// Register your routes here as you build them
// e.g. router.use('/users', require('./user.routes'));

module.exports = router;
